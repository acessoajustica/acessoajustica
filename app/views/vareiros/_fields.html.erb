<% if @vareiro.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@vareiro.errors.count, "error") %> prohibited this vareiro from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @vareiro.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="row">
    <div class="control-group col-md-3 col-sm-6">
        <%= f.label :forum, :class => 'control-label' %>
        <div class="controls">
          <%= f.text_field :forum, :class => 'form-control' %>
        </div>
        <%= error_span(@vareiro[:forum]) %>
      </div>
    </div>
